/**
 * Content Script — Accessibility Tree extraction.
 *
 * Injected into pages to build and return the accessibility tree
 * for use by the MCP server's page reading tools.
 */

console.log('[OpenCode] Accessibility tree content script loaded')
