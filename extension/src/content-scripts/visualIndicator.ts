/**
 * Content Script — Visual Indicator overlay.
 *
 * Injected into pages to display visual feedback for click targets,
 * hover states, and other browser automation actions.
 */

console.log('[OpenCode] Visual indicator content script loaded')
