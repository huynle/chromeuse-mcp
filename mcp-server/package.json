{
  "name": "@opencode-chrome/mcp-server",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server exposing Chrome browser tools to OpenCode",
  "type": "module",
  "scripts": {
    "build": "echo 'TODO: mcp-server build'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@opencode-chrome/shared": "*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
