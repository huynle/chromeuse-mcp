{
  "name": "@opencode-chrome/native-host",
  "version": "0.1.0",
  "private": true,
  "description": "Native messaging host bridging Chrome extension to MCP server",
  "type": "module",
  "bin": {
    "opencode-native-host": "./dist/index.js",
    "opencode-native-host-install": "./dist/install.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@opencode-chrome/shared": "*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
